import { Link } from "react-router-dom";
import "./Menu.css";

const Header = () => {
  return (
    <nav>
      <ul className="nav-items">
        <li>
          <h1>ReQuote</h1>
        </li>

      </ul>
    </nav>
  );
};

export default Header;
